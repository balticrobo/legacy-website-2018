version: '3.7'

services:
  php:
    build: ./docs/docker/php
    volumes:
    - .:/var/www/app
    - ./docs/docker/php/php.ini:/usr/local/etc/php/php.ini
    - ./docs/docker/php:/opt/docker
    environment:
      COMPOSER_ALLOW_SUPERUSER: 1
    working_dir: /var/www/app
    command: /opt/docker/start.sh
  node_assets:
    image: node:10
    user: node
    volumes:
    - .:/var/www/app
    - ./docs/docker/node_assets:/opt/docker
    working_dir: /var/www/app
    command: /opt/docker/start.sh
  nginx:
    image: nginx:1.15
    depends_on:
    - php
    volumes:
    - ./public:/var/www/app/public
    - ./docs/docker/nginx/vhost.conf:/etc/nginx/conf.d/default.conf
    ports:
    - 8000:80
    working_dir: /var/www/app/public
  mysql:
    image: mysql:8.0
    volumes:
    - mysql_data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: mysql
      MYSQL_DATABASE: balticrobo
    ports:
    - 3306:3306
  mailhog:
    image: mailhog/mailhog:v1.0.0
    ports:
    - 8001:8025
    logging:
      driver: none
  node_inky:
    build: ./docs/docker/node_inky
    user: node
    volumes:
    - ./_mail:/var/www/app/_mail
    - ./public:/var/www/app/public
    - ./templates/_mail:/var/www/app/templates/_mail
    - ./docs/docker/node_inky:/opt/docker
    environment:
      DEPLOY_EMAIL_ASSETS_DOMAIN: http://localhost:8000/email/
    ports:
    - 8002:3000
    working_dir: /var/www/app/_mail
    command: /opt/docker/start.sh

volumes:
  mysql_data:
