{% extends '_base.html.twig' %}

{% block body %}
  <div class='wrapper'>
    {% include '_common/header.html.twig' %}
  </div>
  {% include '_common/menu.html.twig' %}
  <div class='wrapper'>
    <div class='container col-12 col-md-6 offset-md-3'>
      {{ form_start(form, {class: 'form', attr: {novalidate: 'novalidate'}}) }}
      <div class='row'>
        <div class='col-12 col-sm-8 offset-sm-2'>
          {{ form_row(form.name) }}
          {{ form_row(form.identifier) }}
        </div>
        <div class='col-12 col-sm-8 offset-sm-2'>
          {{ form_row(form.city) }}
          {{ form_row(form.scientificOrganization) }}
        </div>
        <div class='col-12 col-sm-6 offset-sm-6'>
          <button type="submit" class="btn btn-block btn-primary">{{ 'buttons.add'|trans }}</button>
        </div>
      </div>
      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}
