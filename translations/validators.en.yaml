security:
  Account is locked:
    login: Account is locked.
  Invalid credentials:
    login: Email or password are incorrect.
  Username could not be found:
    login: Email or password are incorrect.
user_register:
  email:
    already_exists: Email already exists. Please log in.
    email: It is not valid email address.
    length:
      max: Maximum email length is {{ limit }} chars.
      min: Minimum email length is {{ limit }} chars.
    not_blank: Enter your email.
  forename:
    length:
      max: Maximum forename length is {{ limit }} chars.
      min: Minimum forename length is {{ limit }} chars.
    not_blank: Enter your forename.
  password:
    length:
      max: Maximum password length is {{ limit }} chars.
      min: Minimum password length is {{ limit }} chars.
    mismatch: Passwords mismatch.
    not_blank: Enter your password.
  surname:
    length:
      max: Maximum surname length is {{ limit }} chars.
      min: Minimum surname length is {{ limit }} chars.
    not_blank: Enter your surname.
  terms:
    is_true: You have to accept terms.
